#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main(char* x){
if (x=="input"){
    printf("%s","placeholder == mette nome dentro a text input /n type        == range(barra spostabile), checkbox(pallino), date, color 
           password, text di default /n)");
}
}